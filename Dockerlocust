# Use the official Locust image as the base image
FROM locustio/locust

# Set the working directory in the container
WORKDIR /app

# Copy the requirements.txt file into the container at /app
COPY src/locust/requirement_locust.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirement_locust.txt

# Copy the Locust test scripts to the working directory
COPY src/locust .

EXPOSE 8089

# Specify the command to run Locust
CMD ["locust", "-f", "locustfile.py", "--host=http://plotly-dash:8050"]
